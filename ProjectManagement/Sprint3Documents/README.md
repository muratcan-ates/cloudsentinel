# Sprint 3 Evidence Pack

*Recaptured 2 August 2026, after the interface rebuild — the July 27 set
showed a design the product no longer has. Taken on a fresh demo stage
(`make demo`: deterministic provider, dates rebased to the current week),
`POST /ops/demo-reset?seed=1`, then one `POST /pulse` so the desk, the
inbox, the agent feed and the review panel carry real chain output.
Full-page captures at 1440px, 2× device scale.*

| File | What it shows |
|---|---|
| `room_watch.png` | Watch room — the desk (everything the estate is holding, filterable by lane), sentinel radar, unified cost/security/fraud watch, sensitivity + mission quick-switch controls |
| `room_investigate.png` | Investigation — 14-day evidence, Analyst's cited triage, Recommender's cautious/bold options with computed savings |
| `room_decide.png` | Decision desk — reflex/conscious split strip, review-panel fold on critical cards, fraud-hold rule-lane cards, HITL verbs |
| `room_intel.png` | Operations intelligence — HITL funnel, approved value, forecast, calibration, self-FinOps ledger, and the market-watch table |
| `room_brain.png` | Brain room — insights, self-review, saved routines, runbook retrieval, detection backtest chart, sign-in |
| `room_broadsheet.png` | The whole broadsheet — every section on one page |
| `swagger_59_endpoints.png` | Self-hosted Swagger, the full 59-path API surface, now reachable from the shared top bar (it used to be a dead end) |

Also here: the Miro Sprint 3 board (`miro_board_sprint3.png`, with two
Done-column details), the burndown (`burndown_sprint3.png`, generated from
the commit record by `scripts/make_burndown.py`), six WhatsApp/Slack
daily-scrum captures, and the AI architecture card used in the demo.
