# Gemini API key from a billing-disabled Google Cloud project (free tier).
# Get one at https://aistudio.google.com/apikey — never commit the real .env.
GEMINI_API_KEY=

# Set to 1 to force the deterministic fake LLM provider (used by tests/CI).
SENTINEL_FAKE_LLM=
